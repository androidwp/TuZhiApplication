<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="item"
            type="com.tuzhi.application.moudle.chooselibtype.ChooseLibTypeItem" />

        <variable
            name="data"
            type="com.tuzhi.application.bean.ItemBean" />

        <import type="android.view.View" />
    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/general_listview_selector"
        android:onClick="@{(view)->item.onItemClick(view,data)}">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="@dimen/generalSpace">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@{data.title}"
                android:textColor="#212121"
                android:textSize="16sp" />

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:src="@drawable/enter"
                android:visibility="@{data.flag?View.VISIBLE:View.GONE}" />
        </LinearLayout>

        <View style="@style/NavigationLine" />
    </FrameLayout>

</layout>