<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="dialog"
            type="com.tuzhi.application.dialog.InputDialog" />

        <variable
            name="title"
            type="String" />

        <variable
            name="text"
            type="String" />
    </data>

    <LinearLayout
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="300dp"
            android:layout_height="wrap_content"
            android:background="@drawable/input_dialog_background"
            android:orientation="horizontal">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:paddingLeft="@dimen/generalSpace"
                android:text="@{title}"
                android:textSize="16sp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:onClick="@{(view)->dialog.onViewClick(view,text)}"
                android:padding="@dimen/generalSpace"
                android:text="完成"
                android:textColor="@color/color71b7fd" />

        </LinearLayout>

        <EditText
            android:id="@+id/et"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:layout_marginTop="4dp"
            android:background="@drawable/input_dialog_background"
            android:gravity="start"
            android:textColor="@color/colorTextHint"
            android:padding="@dimen/generalSpace"
            android:text="@={text}" />

    </LinearLayout>
</layout>
