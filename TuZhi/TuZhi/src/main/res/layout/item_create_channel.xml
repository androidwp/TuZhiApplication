<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="item"
            type="com.tuzhi.application.moudle.repository.knowledgachannel.item.CreateChannelItem" />
    </data>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/generalSpace"
        android:background="@drawable/createchannelbackground"
        android:padding="6dp"
        android:text="新建频道"
        android:onClick="@{()->item.createChannel()}"
        android:drawableLeft="@drawable/searchbox"
        android:drawablePadding="6dp"
        android:textColor="@color/colorWhite"
        android:textSize="12sp" />

</layout>
